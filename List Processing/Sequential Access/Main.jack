class Main{
    function void main(){
        var List v;
        let v = List.new(5,null);
        let v = List.new(3,v);
        let v = List.new(2,v);
        let v = List.new(1,v);
        do v.print();
        do v.dispose();
        return;
    }
}